(function () {

  describe('resourcesController', function () {

  //  beforeEach(module('resources'));

    beforeEach(inject(
      ['$compile','$rootScope', function($c, $r) {
        $compile = $c;
        $rootScope = $r;

      }]
    ));
    it('should display the headline of the directive', function() {

    });
  })
})()