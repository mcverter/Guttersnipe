(function () {
    'use strict';
    // api service test

    describe('API Service', function() {
        beforeEach(function() {});
        describe ('should expose Server Kropotkins that ', function(){
            it (' allows any user to Read a random Kropotkin', function() {})

        });
        describe ('should expose Server Resources that', function(){
            it (' allows a Guest to Read a list of resources ', function() {});
            it (' allows a Guest to Read a specific resource ', function() {});
            it (' allows a Registered User to Create a resource ', function() {});
            it (' allows an Administrator to Delete a resource ', function() {});
            it (' allows an Administrator to Update the Place of a resource ', function() {});
            it (' allows an Administrator to Update the Time of a resource ', function() {});
            it (' allows an Administrator to Update the Thing of a resource ', function() {});

        });
        describe ('should expose Server Users', function(){
            it (' allows a Registered User to Login ', function() {});
            it (' allows a Registered User to Logout ', function() {});
            it (' allows a Guest to Register as a User ', function() {});

        });
    })
})()
