import React from 'react';
import {
  Text,
  View,
  Image,
  StyleSheet
} from 'react-native';

import PropTypes from 'prop-types';

const ShareableSummary = props => {
    return (
        <View>
          <ShareableName />

        </View>
    );
};

const styles = new StyleSheet({

});

ShareableSummary.propTypes = {

};

export default ShareableSummary;
