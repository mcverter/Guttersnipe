import React from 'react';
import {
  Text,
  View,
  Image,
  StyleSheet
} from 'react-native';

import PropTypes from 'prop-types';

const Shareable = ({id, subcategory, category, name, description,
                     address, time, geolocation, icalendar}) => {
    return (
        <View>

        </View>
    );
};
const styles = new StyleSheet({

});

Shareable.propTypes = {

};

export default Shareable;
