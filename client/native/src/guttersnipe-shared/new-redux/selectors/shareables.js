import { createSelector } from "reselect";

export const getCategorizationState = state => {
};
export const subcategoriesSelector = state => {};
export const shareablesSelector = state => {};
export const singleShareableSelector = state => {};
